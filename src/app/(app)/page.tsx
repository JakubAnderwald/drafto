export default function AppPage() {
  // The app shell (notebooks sidebar, note list, editor) is rendered by the (app) layout.
  // This page intentionally renders nothing — all UI comes from AppShell.
  return null;
}
